#!/bin/bash
# Wrapper script for launchd — starts the iMessage watcher
# This script is referenced by the com.rout.imsg-watcher plist.
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec /opt/homebrew/bin/python3 "$SCRIPT_DIR/comms/imsg_watcher.py"
